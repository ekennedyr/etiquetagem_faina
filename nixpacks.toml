# Configuração Nixpacks para o Coolify rodar o backend Node.js (server.js) + Frontend (dist)

[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server.js"
