# Configuração Nixpacks para o Coolify rodar exclusivamente o backend Node.js (server.js) + Frontend
providers = []

[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server.js"

